START
 OPEN WEBSITE
DISPLAY LOG (LOG IN TAG);
PRINT (HAVE AN ACCOUNT?) { 
 IF (YES SELECTED)
}
 PRINT ("LOG IN")
 PRINT ("ENTER PASSWORD")
 IF (CORRECT PASSWORD ENTERED)
 ELSE IF PRINT (ERROR) ); 
 {
 PRINT (LOGGED INTO SYSTEM)
 INPUT  ("ARE YOU A CUSTOMER OR EMPLOYEE?");
 IF (CUSTOMER SELECTED) {
 PRINT ("CUSTOMER PROFILE ACCESED");
 ELSE IF (PRINT ERROR) {
 DISPLAY 
 (STORE ACCESSED, BROWSE PRODUCTS, COMMENT ON PRODUCT, SELECT PRODUCT")
        IF } ("ONLINE PURCHASE,DELIVERY PAYMENT")
        PRINT ("ONLINE PURCHASE")
        ELSE IF ("DELIVERY PAYMENT"); 
 }
         end IF
DISPLAY (SALES PLACED,SALES MADE
        ,ORDER PLACED");
        IF (REFRESH PAGE);
PRINT (("RECEIPT")
ELSE IF {
PRINT ("ERROR")
PRINT "("OUT OF STOCK")
DIPLAY (CUSTOMER PROFILE,PURCHASING HABITS,ENQUIRY SECTION));
END IF
}
IF (EMPLOYEE SELECTED) {
PRINT ("EMPLOYEE PROFILE ACCESSED") 
}
DISPLAY (STORE ACCESSED,DAILY TASKS, CUSTOMER INTERACTION SECTION
{
IF "PROFILE PAGE IS REFRESHED){
PRINT 
DISPLAY ((BROWSE STORE,LOG OUT)
ELSE}
 IF LOG OUT SELECTED (PRINT LOGGED OUT));
 {
END IF
 }
