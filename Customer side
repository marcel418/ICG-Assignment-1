Start
Print "Are you male or female?"
 Get Gender
   if(Gender=female) then
      Print " These are the recommended products for woman"
       Get femalep_roducts
   if(Gender=male) then
      Print "These are the recommended products for men"
       Get male_products
    endif
endif
   if(product(n)) then
     Print product(n)_desc
     Print product(n)_price
    
endif
Stop
     
     
      




